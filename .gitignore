.DS_Store
*.log
# Sorties générées (régénérables via contenu/build)
contenu/**/*.pdf
contenu/**/*.html
# Clés locales — NE JAMAIS committer
config.js
.env

# PDF exportés (produit payant) — ne pas committer dans un dépôt public
pdf/
