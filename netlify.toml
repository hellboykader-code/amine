# Configuration Netlify — app React (Vite) MK Phone
[build]
  command = "npm run build"
  publish = "dist"

# SPA : rediriger toutes les routes vers index.html (HashRouter fonctionne aussi sans ça)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
